import 'package:flutter/material.dart';

class AvaliableDates extends StatelessWidget {
  const AvaliableDates({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
